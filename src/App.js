import React, { Component } from 'react';

export default class App extends Component {
  render() {
    return (
      <div id="app">
        <canvas id="sprite-canvas" />
        <div id="script-test" />
      </div>
    );
  }
}
